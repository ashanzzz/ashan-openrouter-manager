export type RankingMode='intelligence_first'|'weighted'
export interface Settings {openrouter_api_base:string;openrouter_upstream_base:string;newapi_base_url:string;newapi_admin_user_id:string;alias_model:string;managed_group:string;managed_tag:string;channel_name_prefix:string;min_context_length:number;candidate_pool:number;preflight_concurrency:number;require_benchmark:boolean;require_free_suffix:boolean;include_models:string[];exclude_models:string[];ranking_mode:RankingMode;intelligence_weight:number;coding_weight:number;agentic_weight:number;context_weight:number;auto_sync:boolean;sync_interval_minutes:number;channel_type:number;enabled_status:number;disabled_status:number;priority_base:number;priority_step:number;channel_weight:number;auto_ban:boolean;e2e_test_enabled:boolean}
export interface RankedModel {rank:number;id:string;name:string;context_length:number;intelligence_index?:number|null;coding_index?:number|null;agentic_index?:number|null;score:number;usable?:boolean|null;test_error?:string|null}
export interface Scan {scanned_at:string;total_models:number;free_models:number;ranked_candidates:RankedModel[];selected:RankedModel[];warning?:string|null}
export interface Channel {rank:number;channel_id:number;name:string;model_id:string;priority:number;updated_at:string}
export interface Run {id:string;started_at:string;ended_at:string;trigger:string;status:string;changed:boolean;selected_models:string[];error?:string|null}
export interface Status {healthy:boolean;configured:boolean;current_models:Channel[];last_scan?:Scan|null;last_run?:Run|null;scheduler:{next_run_at?:string|null;enabled:boolean};secrets:{openrouter_api_key:boolean;newapi_admin_token:boolean;newapi_test_token:boolean}}
