# Keep APP_MASTER_KEY stable after secrets have been saved.
APP_MASTER_KEY=replace-with-a-long-random-string
# Host-side WebUI port only. The container always listens on 8080 internally.
WEBUI_PORT=8080
RUST_LOG=info,ashan_openrouter_manager=debug
